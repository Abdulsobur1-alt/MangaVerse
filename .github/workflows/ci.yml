name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Typecheck & Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      # pnpm 11 hard-fails image builds on any unapproved build script
      # (ERR_PNPM_IGNORED_BUILDS). The Dockerfiles rebuild pnpm-workspace.yaml
      # from an inline trimmed allowBuilds map — fail fast if it drifts from
      # the root workspace instead of discovering it in a deploy.
      - name: Dockerfile allowBuilds in sync with workspace
        run: node scripts/check-docker-allowbuilds.mjs

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate Prisma schema
        run: pnpm --filter @mangaverse/api exec prisma validate

      - name: Typecheck (turbo)
        run: pnpm turbo typecheck

      - name: Run API unit tests
        run: pnpm --filter @mangaverse/api test

      - name: Run web unit tests
        run: pnpm --filter @mangaverse/web test
